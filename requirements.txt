# Core image processing and detection (detect_cones.py, image_recognition.py, analyze_seneca.py)
numpy>=1.19.0
Pillow>=8.0.0
scipy>=1.7.0

# Interactive cone labeler display (label_cones.py)
matplotlib>=3.3.0

# SVM cone classifier — optional, only needed with --classifier flag (image_recognition.py)
scikit-learn>=1.0.0

# Development / testing
pytest>=6.2.5

# Blender is required for 3D scene generation (not installable via pip)
# Download from: https://www.blender.org/download/
# Run headlessly: blender --background file.blend --python blender_place_cones.py -- --json cones.json
